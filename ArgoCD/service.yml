apiVersion: v1
kind: Service
metadata:
  name: swiggy-app
spec:
  type: NodePort   # (use this if Minikube)
  selector:
    app: swiggy-app
  ports:
  - port: 80
    targetPort: 80     # ✅ FIXED
    nodePort: 30007
